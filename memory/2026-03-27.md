# 2026-03-27 工作日志

## 完成的工作

### 1. Trilium 笔记备份 ✅
- 从 Windows 节点的 root.zip 导出 Trilium 笔记
- 成功提取 20 篇 HTML 笔记（部署文档、Jenkins、K8s、MySQL备份等）
- 保存到 memory/trilium_backup.md (521KB)
- 同步到 GitHub: hhhhhyz-11/ai-memory (commit 76225c2, force push)

### 2. Jenkins 流水线添加钉钉通知 ✅
- 参考模板帮老大完善钉钉机器人通知功能
- 踩坑：dingtalk 插件 text 参数要传 List<String>，不能传 String
- 修复方式：去掉 .join('') 改成列表格式

### 3. 技术问答
- **K8s 证书更新**：确认不需要重启 Docker，只需 kubeadm certs renew + kubectl rollout restart
- **Nacos 集群新节点上线**：raft 目录要为空，不能有旧数据
- **MySQL 主从复制**：状态正常，Slave_IO_Running: Yes, Slave_SQL_Running: Yes, 无延迟

### 4. 遗留问题
- Jenkins SSH 凭证失效（192.168.0.180 的 SSH 密码/密钥需要更新）

## 技术收获

- Jenkins DingTalk 插件：text 参数类型是 List<String>，不是 String
- Trilium 数据库 WAL 模式：Trilium 运行时无法直接读 DB，主文件只有系统笔记，用户笔记在 WAL 里
  - 解决方案：Trilium 导出 root.zip 获取 HTML 格式备份
