# 2026-03-12 工作日志

## 完成的工作

### 1. 节点问题
- **H节点掉线**：帮助用户在Windows上重新安装并启动OpenClaw Node
- 问题原因：环境变量 `OPENCLAW_GATEWAY_TOKEN` 未设置
- 解决：设置 token 后节点恢复正常

### 2. 技术博客汇总
- **Redis集群水平扩展**：从CSDN文章汇总，保存到 `learnings/redis-cluster-horizontal-scaling.md`
- **Vagrant从入门到超神**：汇总保存到 `learnings/vagrant-quick-start.md`

### 3. Jira 问题排查
- 用户访问 `brwse/BIKE-11560` 链接失败
- 原因：URL 拼写错误（brwse → browse）
- 解释：这是 Jersey 框架警告，不影响功能

### 4. Jenkins 部署问题
- 问题：rsync 找不到 `/home/jenkins/src/workspace/uav-defence-web/dist/` 目录
- 原因：workspace 是 `uav-defence-web@2`（之前构建被强制中止导致）
- 解决：修改 yml 配置使用正确的 workspace 路径

### 5. SonarQube + GitLab CI 增量扫描
- 需求：MR 合并到 uat 时触发增量代码扫描
- 配置：推送代码到 feature 分支时自动触发 SonarQube 扫描
- 使用 `-Dsonar.newCode.referenceBranch=uat` 进行增量对比
- GitLab 版本 11.1.4 不支持 `rules` 和 `only: merge_requests`，用 shell 脚本判断

### 6. SonarQube PDF 报告问题
- 问题：PDF 生成成功但上传失败
- 解决：排查权限和磁盘空间问题
- 另外创建了 sonar_pb_to_html.sh 脚本，可从 Protobuf 导出生成 HTML 可视化报告

### 7. 学习资料
- 汇总了 Docker Compose 安装 SonarQube + 多分支插件的文章
- 汇总了 SonarQube + GitLab 增量扫描与提交阻断的文章

## 待办
- [ ] 优化 SonarQube 增量扫描（只扫描变更文件）
