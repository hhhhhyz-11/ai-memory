# 2026-03-23 工作日志

## 完成的工作

### 1. GitLab CI/CD 流水线完善

- **SonarQube 扫描配置**
  - 增量代码扫描
  - 质量门检查
  - 获取新增问题详情（Bug、漏洞、异味）

- **PDF 报告生成**
  - 使用 Python + WeasyPrint 生成中文 PDF 报告
  - 包含问题详情：文件、行号、严重性、描述
  - 按类型分类显示

- **MinIO 上传**
  - 配置 MinIO 客户端 mc
  - 自动创建 bucket
  - 设置公开访问
  - PDF 上传到指定目录结构

- **MR 评论与通知**
  - 自动发送 MR 评论（包含质量门结果和 PDF 链接）
  - 失败时发送钉钉通知

### 2. Docker 镜像构建

- Dockerfile.sonar 包含：
  - Maven 3.9.6 + JDK 11
  - Python 3 + jinja2 + weasyprint
  - MinIO Client (mc)
  - 中文字体支持

### 3. GitHub 仓库管理

- 创建项目仓库：https://github.com/hhhhhyz-11/project
- 上传文件：
  - `gitlab/sonar-uat.yml` - UAT 流水线（自动）
  - `gitlab/sonar-master.yml` - Master 流水线（手动触发）

## 问题解决

1. **MinIO 端口问题** - 9001 是 Console 端口，需要用 9000 API 端口
2. **Bucket 权限问题** - 需要设置公开访问或使用 policy

## 需要记录的变量

GitLab CI 变量：
- `SONAR_UAT` - UAT 项目 Token
- `SONAR_MASTER` - Master 项目 Token
- `MERGE_TOKEN` - GitLab PAT
- `DINGTALK_WEBHOOK` - 钉钉 Webhook
- `MINIO_ACCESS_KEY` - MinIO 用户名
- `MINIO_SECRET_KEY` - MinIO 密码
