# 2026-03-09 学习总结

> 日期：2026-03-09
> 星期：周一

## 📝 今日概览

完成工作空间初始化、Windows 节点配置、Gateway 暴露外部访问。

## 🛠 技术知识

### OpenClaw Gateway 配置
- **bind 模式**: `loopback` → `custom` + `customBindHost: "0.0.0.0"` 允许外部连接
- **端口**: 18789
- **Token 认证**: `614a1c77db033b45b178454d632d3aca309d88b3e0975b83`

### 节点配对 (Node Pairing)
- 节点通过 WebSocket 连接到 Gateway
- 需要配对批准：`openclaw devices approve <requestId>`
- 使用 `OPENCLAW_GATEWAY_TOKEN` 和 `OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=1` 环境变量
- Windows 节点使用 Scheduled Task 安装服务

### 定时任务 (Cron)
- OpenClaw 内置 cron 支持
- 使用 `openclaw cron add` 创建定时任务
- 支持上海时区 `tz: "Asia/Shanghai"`
- 任务执行会发消息到主会话 (announce 模式)

## 🔧 踩坑记录

1. **gateway bind 不生效**: 
   - 原因：`lan` 模式不支持
   - 解决：使用 `custom` + `customBindHost: "0.0.0.0"`

2. **节点配对请求收不到**:
   - 原因：使用了错误的命令 `openclaw nodes approve`
   - 解决：使用 `openclaw devices approve`

3. **Windows 环境变量设置**:
   - CMD 使用 `set VAR=value`
   - PowerShell 使用 `$env:VAR="value"`
   - 一行命令需要用 `VAR=value cmd` 格式

## 💡 经验教训

- 初始化流程：BOOTSTRAP.md → IDENTITY.md / USER.md / SOUL.md → 删除 BOOTSTRAP.md
- 配对节点用 `openclaw devices` 而不是 `openclaw nodes`
- 外部 ws:// 连接需要设置 `OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=1`

## 📅 明日待办

- [ ] 继续完善工作空间配置
- [ ] 测试节点功能

---

*自动生成于 2026-03-09 14:41*
