# KingBaseES V9R2C4 主备集群部署

> 部署时间：2026-04-08
> 版本：V009R002C014B0009
> 部署环境：Vagrant 虚拟机 (CentOS 7)

---

## 一、环境信息

| 项目 | KingBase1 | KingBase2 |
|------|-----------|-----------|
| 主机名 | kingbase1 | kingbase2 |
| IP | 192.168.56.11 | 192.168.56.12 |
| VIP | 192.168.56.15/24 | - |
| 安装用户 | kingbase | kingbase |
| 数据库端口 | 54321 | 54321 |
| securecmdd 端口 | 8890 | 8890 |

---

## 二、部署前准备

### 1. 创建 kingbase 用户

```bash
# 两台节点都需要执行
groupadd kingbase
useradd -g kingbase -m -d /home/kingbase -s /bin/bash kingbase
echo "kingbase:kingbase" | chpasswd
```

### 2. 初始化用户目录

```bash
cp -a /etc/skel/. /home/kingbase/
chown -R kingbase:kingbase /home/kingbase
```

### 3. 检查/设置服务器语言

```bash
echo $LANG
export LANG=zh_CN.UTF-8
```

### 4. 更新 /etc/hosts

```bash
# 两台节点都需要执行
echo "192.168.56.11 kingbase1" >> /etc/hosts
echo "192.168.56.12 kingbase2" >> /etc/hosts
```

---

## 三、单机库部署

### 参考文档

详细步骤参考：[单机部署全流程](https://github.com/hhhhhyz-11/ai-memory/blob/master/kingbase-v9r2c4/01-%E5%8D%95%E6%9C%BA%E9%83%A8%E7%BD%B2%E5%85%A8%E6%B5%81%E7%A8%8B-2026-04-07.md)

---

## 四、集群部署

### 1. 创建目录结构（重要！）

**⚠️ 必须先创建 Server 目录，再解压 db.zip**

```bash
mkdir -p /home/kingbase/cluster/kingbase/Server
mkdir -p /home/kingbase/cluster/oracle
```

### 2. 复制并解压 db.zip

**在 KingBase1 上执行：**

```bash
cd /home/kingbase/cluster/kingbase/

# 复制 license.dat 和 db.zip
cp ./kingbase/KESRealPro/V009R002C014/Server/bin/license.dat Server/
cp db.zip Server/

# 复制到 KingBase2
scp Server/license.dat kingbase@192.168.56.12:/home/kingbase/cluster/kingbase/Server/
scp Server/db.zip kingbase@192.168.56.12:/home/kingbase/cluster/kingbase/Server/
```

### 3. 解压 db.zip

**两台节点都需要执行：**

```bash
cd /home/kingbase/cluster/kingbase/Server/
unzip db.zip
```

**解压后目录结构：**
```
/home/kingbase/cluster/kingbase/
└── Server/
    ├── bin/
    ├── include/
    ├── lib/
    ├── share/
    ├── log/
    └── db.zip
```

### 4. 配置 install.conf

**文件路径：** /home/kingbase/cluster/oracle/install.conf

```conf
all_ip=(192.168.56.11 192.168.56.12)
install_dir="/home/kingbase/cluster/kingbase"
zip_package="/home/kingbase/cluster/kingbase/Server/db.zip"
license_file=(license.dat)
db_user="system"
db_password="kingbase"
db_port="54321"
db_auth="scram-sha-256"
db_checksums="yes"
archive_mode="always"
encoding="UTF8"
locale="zh_CN.UTF-8"
trusted_servers="192.168.56.1"
virtual_ip="192.168.56.15/24"
net_device=(eth1 eth1)
net_device_ip=(192.168.56.11 192.168.56.12)
deploy_by_sshd=0
```

### 5. 配置 SSH 互信

```bash
cd /home/kingbase/cluster/kingbase/Server/
sh trust_cluster.sh
```

### 6. 配置 securecmdd 服务

**两台节点都需要执行：**

```bash
cd /home/kingbase/cluster/kingbase/Server/bin/
./sys_HAscmdd.sh init
./sys_HAscmdd.sh start
netstat -tlunp |grep 8890
```

### 7. 验证 securecmdd 互信

```bash
./sys_securecmd root@192.168.56.12 'whoami'
./sys_securecmd kingbase@192.168.56.12 'whoami'
```

### 8. 关闭防火墙（测试环境）

```bash
systemctl stop firewalld
systemctl disable firewalld
```

### 9. 执行集群部署

```bash
su - kingbase
cd /home/kingbase/cluster/oracle
sh cluster_install.sh
```

---

## 五、部署结果

### 集群状态

```
 ID | Name  | Role    | Status  | Upstream | repmgrd | PID   | Paused?
----+-------+---------+---------+----------+---------+-------+--------
 1  | node1 | primary | * running |          | running | 12650 | no
 2  | node2 | standby | running  | node1    | running | 5041  | no
```

---

## 六、常见问题

### 问题1：目录结构错误

**原因：** 未提前创建 Server 目录，db.zip 解压到错误位置

**预防方法：** 严格按照本文档步骤1执行，先创建 Server 目录再解压

**解决：**
```bash
cd /home/kingbase/cluster/kingbase/
mkdir -p Server
mv bin include lib share Server/
```

### 问题2：ping 不通网关

**解决：**
```bash
systemctl stop firewalld
systemctl disable firewalld
```

---

## 七、⚠️ 注意事项

1. **目录结构**：必须先创建 Server 目录，再解压 db.zip
2. **安装用户**：集群部署脚本必须用 kingbase 用户执行
3. **防火墙**：测试环境建议关闭防火墙
4. **trusted_servers**：需配置为可达的网关 IP，或留空
